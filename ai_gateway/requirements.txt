# =============================================================================
# AI Gateway - Python Dependencies
# =============================================================================
# Install:
#   python -m pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
#   pip install -r requirements-stt.txt --no-build-isolation
#
# Whisper is in requirements-stt.txt because pip's *isolated* build runs Whisper's
# setup.py before pkg_resources is visible → use --no-build-isolation (Windows-safe).
#
# Python 3.14 note: scikit-learn ≥1.7.2 and torch ≥2.9.0 are required because
# older pinned versions have no pre-built wheels for CPython 3.14 and require
# a C compiler (cl / gcc / clang) which is not present on this machine.

setuptools>=69.0.0,<82.0.0
wheel

# Web Framework
flask==3.1.1
flask-cors==5.0.1
gunicorn==23.0.0

# Google Gemini (Answer Generation)
google-generativeai==0.8.5
python-dotenv==1.1.0

# NLP Evaluation (Sentence-BERT)
sentence-transformers==3.4.1
scikit-learn==1.7.2

# Speech-to-Text (Whisper) → see requirements-stt.txt

# Voice Analysis (Wav2Vec2 + librosa)
transformers==4.48.3
librosa==0.10.2.post1
soundfile==0.13.1
scipy==1.16.1

# Facial Analysis (DeepFace)
deepface==0.0.93
opencv-python==4.11.0.86

# Common
torch==2.9.1
numpy>=2.2.3
